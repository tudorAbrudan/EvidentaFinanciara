# Imagine minimă: zero dependențe npm, deci nu e nevoie de `npm install`.
FROM node:22-alpine

WORKDIR /app
COPY package.json ./
COPY *.js ./

ENV NODE_ENV=production
ENV PORT=8080
EXPOSE 8080

USER node
CMD ["node", "server.js"]
